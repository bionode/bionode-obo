'use strict'

/**
 * Parse OBO files. Emits a `term` object stream.
 * @module bionode-obo
 */


/**
 * Parse OBO 1.2 file
 * @return {string} the parsed output
 */
exports.parse = function () {
  return 'foo'
}
